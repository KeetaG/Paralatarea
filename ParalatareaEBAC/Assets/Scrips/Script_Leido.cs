using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script_Leido : MonoBehaviour
{
    public float ValorGuardado = 15.2f;
    public bool BoolGuardado = true;
    public GameObject GoGuardado;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
